{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": ""
  },
  "deploy": {
    "startCommand": "libretranslate --host 0.0.0.0 --port 8080 --load-only en,fr,es,vi,tl,tr,ar,zh,ja,ko,id",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "always"
  }
}
